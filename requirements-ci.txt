# CI dependencies (keep lightweight and avoid heavy optional ML/visualization libs)

# Runtime deps required by imports/tests
PyYAML>=6.0
types-PyYAML>=6.0
typer>=0.9.0
rich>=13.0.0
requests>=2.31.0
jsonschema>=4.19.0

# Infra scanning libs used by scanners
kubernetes>=28.1.0
docker>=6.1.0
python-hcl2>=4.3.0

# ML engine (uses scikit-learn + joblib; avoids tensorflow)
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0
# Note: xgboost 2.0+ and lightgbm 4.0+ require Python 3.8+
# For Python 3.9 compatibility, ensure pip and setuptools are up to date
xgboost>=2.0.0
lightgbm>=4.0.0
pandas>=2.0.0

# Platform dependencies
websockets>=11.0.0
semver>=3.0.0

# Policy engine
# Note: opa-python is not actually used in the codebase (see policy.py comments)
# It requires Python 3.10+, so removed from CI requirements for Python 3.9 compatibility
# opa-python>=0.0.9

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-timeout>=2.1.0
pytest-mock>=3.12.0
coverage>=7.3.0

# Lint / formatting / typecheck
black>=23.11.0
isort>=5.12.0
flake8>=6.1.0
mypy>=1.7.0

# Security tools
bandit>=1.7.0
safety>=2.3.0
